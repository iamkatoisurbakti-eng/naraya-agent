# KSR888 live container sync

When editing the running KSR888 site:
- Host repo changes may not appear in the web container automatically.
- Copy the modified PHP/Blade file into `nusantara-ai-saas-ksr888-web-1`.
- Restart the web container before final verification.

Use this when a local fix looks correct but the live site still serves old HTML.
